info:
  name: github_login
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/v1/public/auth/github_login"
  body:
    type: json
    data: |-
      {
        "code": "4/0AfJohXn..."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
