info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/v1/public/auth/login"
  body:
    type: json
    data: |-
      {
        "username":"f4ran",
        "email":"",
        "mobile":"",
        "password":"vitalize"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
