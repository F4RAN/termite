info:
  name: get-user-posts
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/v1/posts/:username/posts"
  params:
    - name: username
      value: f4ran
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
