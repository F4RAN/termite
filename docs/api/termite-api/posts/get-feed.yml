info:
  name: get-feed
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/api/v1/posts/feed"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
