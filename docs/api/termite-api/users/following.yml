info:
  name: following
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/api/v1/users/:username/following"
  params:
    - name: username
      value: f4ran
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
