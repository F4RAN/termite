info:
  name: update-profile
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base_url}}/api/v1/users/me"
  body:
    type: json
    data: |-
      {
        "username":"",
        "nickname":"",
        "avatar":"media_id_1",
        "header":"media_id_2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
