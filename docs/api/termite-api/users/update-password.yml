info:
  name: update-password
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/api/v1/users/password"
  body:
    type: json
    data: |-
      {
        "old_password":"vitalize",
        "new_password":"new_vitalize"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
