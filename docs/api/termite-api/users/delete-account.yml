info:
  name: delete-account
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/api/v1/users/me"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
