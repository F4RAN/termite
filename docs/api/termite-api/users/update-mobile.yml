info:
  name: update-mobile
  type: http
  seq: 5

http:
  method: PUT
  url: "{{base_url}}/api/v1/users/mobile"
  body:
    type: json
    data: |-
      {
        "new_mobile":"+15712222222",
        "otp":"123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
