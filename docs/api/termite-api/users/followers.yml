info:
  name: followers
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/api/v1/users/:username/followers"
  params:
    - name: username
      value: f4ran
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
